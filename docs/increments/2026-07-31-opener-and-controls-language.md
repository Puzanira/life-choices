# Increment: опенер по эталону + язык контролов (зелёная = подтверждение)

<!-- ВИЗУАЛ+UX. Программа встречи 2026-07-29, вставной инкремент (между 2/7 и 3/7),
     поднят по запросу основательницы «стартовый экран планируется?» + её решению
     99fab3c. Baseline: 6de7573. -->

## 1. Пересказ дизайна

Два сросшихся куска (оба бьют в одни экраны/тексты):

- **Экран A «Опенер»** по эталону `explainers/Стартовый экран.png` (⚠ эталон
  2752×1536 — сверять с пересчётом ×0.6977, asset-map §11-13): логотип шоу
  (вырезать из эталона спрайтом `opener-logo-v2` — отдельного файла в паке нет),
  кремовая плашка правил в жёлтой марки-рамке с лампочками, канон-текст правил
  (build-spec §A, БЕЗ «5 секунд»), подсказка старта «НАЧАТЬ ЖИЗНЬ» на зелёной.
  HUD/таймер на опенере не идут (как сейчас).
- **Язык контролов + зелёное подтверждение** (решение основательницы 99fab3c):
  1) финал: «НАЧАТЬ ЗАНОВО» = **GreenButton** (AnswerYes); красная на финале
     инертна; dev-клавиша CONFIRM (Enter) продолжает работать скрыто;
  2) ВСЕ игровые тексты называют ФИЗИЧЕСКИЕ контролы, слово «Enter» из подсказок
     исчезает: «НАЧАТЬ ЖИЗНЬ (Enter)» → «...— жми ЗЕЛЁНУЮ», «НАЧАТЬ ЗАНОВО
     (Enter)» → «...— жми ЗЕЛЁНУЮ», туториал ребёнка «Жмите Enter, пока горит»
     → «Жмите «!», пока горит» (кнопка BangButton), прочие вхождения — по грепу.

## 2. Done contract

| # | Class | Criterion |
|---|-------|-----------|
| 1 | boots | Старт ок, компиляция чистая. |
| 2 | player path | Опенер: логотип + плашка правил канон-текстом + «НАЧАТЬ ЖИЗНЬ» зелёной; старт по GreenButton → I02. |
| 3 | interaction path | Финал: рестарт по GreenButton; RedButton на финале ничего не делает; dev-Enter работает как прежде. |
| 4 | failure/restart path | Полный цикл смерть→финал→зелёная→опенер→старт без регрессий. |
| 5 | observability | Ни одной игровой подсказки со словом «Enter»/dev-клавишами; все называют физический контрол. |
| 6 | design | Опенер против эталона (×0.6977): композиция/логотип/плашка совпадают; текст правил целиком в плашке. |
| 7 | regressions + Слой-2 | Тесты: финал-рестарт на AnswerYes + инертность AnswerNo; «no-Enter»-текст-гард по всем игровым строкам; опенер-конформанс. Suite HEADLESS зелёный. |

## 3. Approved by founder

`Approved by founder:` 2026-07-29/31 — «стартовый экран планируется?» + решение
«подтверждение по зелёной, у нас другие контроллеры» (закоммичено 99fab3c).

## 4. Checkpoint log

- **(a) start:** дерево чистое (только этот файл untracked), baseline `b271f09`
  (bookkeeping поверх `6de7573`). Предыдущий запуск оборвался до правок — дубля нет.
- **(b) замер эталона (PIL, ×0.6977):** `Стартовый экран.png` 2752×1536.
  Логотип `src (639,59)-(2096,742)` → **1920: центр (954,279), 1017×477**;
  марки-рамка (внешний тёмный кант) `src x97..2674 y745..1493` → **центр (967,781),
  1798×522**; кремовая плашка `src x150..2624 y797..1442` → **центр (968,781),
  1726×450**; лампочки по осевой золотой полосы **x87..1849 / y538..1024**, шаг
  37.5 / 42.1, ядро ≈16 (с ободком ≈22–24); текст правил — 5 строк, шаг 63,
  обводка/капитель 0.135 (≈Rubik Regular, у нас есть только 300 и 700).
- **(c) логотип вырезан:** матовка = заливка фона по ЗАМКНУТОМУ тёмному контуру
  (`max(RGB)<95`) в окне x560..2200 y15..743 → крупнейшая компонента; premultiply →
  LANCZOS до 1017×477 (рисуется 1:1 на кабинетных 1920×1080) → unpremultiply.
  `Art/Resources/Sprites/opener-logo-v2.png` + `.meta` по образцу `sunburst-bg-v3`
  (spriteMode 1, PPU 100, mip off, Compression None, свежий GUID).
- **(d) язык контролов (канон 99fab3c):** `OnInput` — GREEN(`AnswerYes`) = единственный
  confirm на опенере / хинте / **финале**; ветка `AnswerNo`+Finale удалена, красная на
  финале инертна (чистый `Game` и так игнорирует `AnswerNo` вне Playing — новых веток
  в Game.cs не потребовалось). Депрессия-ловля (GREEN во время Playing) не тронута,
  dev-CONFIRM работает как прежде.
- **(e) тексты:** «Enter» в видимых строках не осталось ни одного (grep по строковым
  литералам всех Scripts пуст). Заодно убраны прочие dev-клавиши (критерий 5):
  ПРОБЕЛ→РУЧКА, E→ДАТЧИК ВЫСОТЫ, ↑/↓→ДЖОЙСТИК ВВЕРХ/ВНИЗ, формулировки сверены с
  канон-текстами `host-content.md §4` («верти ручку», «датчик высоты», «жми “!”»).
- **(f) опенер пересобран:** лучи (общий `sunburst-bg-v3`) → логотип в своём боксе →
  марки-рамка слоями (кант `Ink` → золото `#f8b432` → кант → крем `#f7e4bb`) + 118
  лампочек `marquee-bulb` по замеренной осевой → канон-текст правил (Rubik, `Ink`,
  best-fit ≤46, вес добит однопиксельным Outline того же цвета) → зелёная CTA-плашка
  «НАЧАТЬ ЖИЗНЬ — ЖМИ ЗЕЛЁНУЮ» (Arimo Bold) внизу кремовой. Старые четыре кобальтовые
  плашки правил и текстовый заголовок сняты — их роль отдана логотипу и канон-тексту.
  HUD/таймер на опенере по-прежнему не идут.
- **(g) тесты:** новый `ControlLanguageTests` (GREEN рестартит / RED инертна+не мнёт
  некролог / dev-CONFIRM жив / хинт закрывается зелёной / no-dev-key гард рефлексией
  по строковым полям драйвера + по всем `Text` в иерархии, включая неактивные панели);
  `ScreensConformanceTests.Opener_*` переписан на конформанс эталону (боксы ±10,
  кольцо лампочек, канон-текст, CTA называет ЗЕЛЁНУЮ, точная перепись спрайтов);
  `ArcadeExitTests` — RED на финале теперь проверяется как ИНЕРТНАЯ, рестарт зелёной;
  снят устаревший `StringAssert.Contains("Enter", …)` с кнопки «ПОНЯТНО».
  `ArcadeScreenshotTests` — добавлены позы `opener` и `finale`.
- **(h) первый прогон suite — КРАСНЫЙ (exit 2), и это поймал новый тест, а не старый:**
  EditMode 243/243, PlayMode 79/80, упал `ControlLanguageTests.Finale_Green_Restarts…`
  («Expected Opener, but was Finale»). Причина настоящая и полезная: синхронный
  drive-loop гасит хинты в одном кадре и оставляет взведённым `_dismissedThisFrame`
  (страж «съесть всё, кроме Confirm, в кадре закрытия хинта») — он чистится в
  `Update`, которого в тугом цикле нет. Правка в ТЕСТЕ (`yield return null` перед
  проверкой), не в продакшене: на кабинете страж отрабатывает штатно, что и
  доказывает зелёный `ArcadeExitTests` (реальный путь FakeBackend → ArcadeInputSource
  → GREEN рестартит, RED инертна) — он прошёл с первого раза.
- **(i) кадр опенера + сверка (раунд 1):** три расхождения с эталоном, все починены
  слопом, не ослаблением: (1) автоперенос рвал 2-е предложение не там и оставлял
  куцую строку → переносы канон-текста расставлены вручную по 5 строкам эталона;
  (2) на логотипе всплыла серая соринка самого эталона (≈39×13 px у нижнего контура,
  топологически приклеена к обводке, поэтому «выбросить мелкий остров» не помогало) →
  вычищена по цвету в окне и матовка пересобрана; (3) плашки красились «в токен»
  напрямую, а `bar-track` не белый (#E7E9F5) — золото выходило 224,164,47 вместо
  248,180,50 → добавлен `OnBarTrack()`.
- **(j) раунд 2 — своя же ошибка, поймана глазом:** первая версия `OnBarTrack`
  клампила канал; у крема #F7E4BB красный упирается в 231, а зелёный (228) нет —
  R и G сравнялись и плашка ушла в **хаки**. Переделано на пропорциональное гашение
  (общий множитель по самому тесному каналу): тон сохранён, крем садится на 6.5 %
  темнее токена — предел этого спрайта.
- **(k) кадр опенера — сверка (финал).** `LIFECHOICES_SHOT_POSE=opener` →
  `…/42c90134-…/scratchpad/inc-opener-shot.png` (15:06:20, свежее последней правки
  исходников 15:05:52), смотрел глазами + скрипт
  `…/afc9f354-…/scratchpad/verify_final.py` (одинаковые маски на эталоне и на рендере):

  | Проверка | Эталон ×0.6977 | Рендер 1920×1080 | Δ | Допуск |
  |---|---|---|---|---|
  | Марки-рамка (золото) x | 70..1861 | **71..1860** | +1 / −1 | ±10 ✓ |
  | Марки-рамка (золото) y | 523..1037 | **524..1037** | +1 / 0 | ±10 ✓ |
  | Кремовая плашка x | 105..1827 | **107..1824** | +2 / −3 | ±10 ✓ |
  | Кремовая плашка y | 557..1003 | **560..1001** | +3 / −2 | ±10 ✓ |
  | Логотип (красный контур) x | 539..1392 | **539..1391** | 0 / −1 | ±10 ✓ |
  | Логотип (красный контур) y | 63..447 | **63..446** | 0 / −1 | ±10 ✓ |
  | Логотип, центр | (966, 255) | **(965, 254)** | −1 / −1 | ±10 ✓ |
  | Лампочки, верхний ряд | 48 шт, первая 87 / последняя 1849 | **48 шт, 86 / 1846** | −1 / −3 | ±10 ✓ |
  | Крем (цвет) | 247,228,188 | **231,213,175** | −6.5 % по тону | предел `bar-track` |
  | Золото (цвет) | 248,180,50 | **231,167,47** | −6.5 % по тону | предел `bar-track` |
  | Зелёная CTA | токен #05CE51 | **0,205,81** | ≈0 | ✓ |
  | Текст правил (чернила) | x174..1737 y630..931 | **x308..1622 y591..840** | блок на ~16 % меньше и выше | осознанно: CTA живёт в той же плашке |

  `magenta=0`. Единственное осознанное отступление от эталона — CTA «НАЧАТЬ ЖИЗНЬ —
  ЖМИ ЗЕЛЁНУЮ» внизу кремовой плашки: на эталоне её нет вовсе, а под рамкой остаётся
  38 px до низа экрана — кнопку туда не поставить, поэтому канон-текст ужат до
  5 строк ~43 px (эталон ~52) и разделил плашку с зелёной кнопкой.
- **(l) кадр финала.** `LIFECHOICES_SHOT_POSE=finale` (поза добавлена в харнесс) →
  `…/42c90134-…/scratchpad/inc-finale-shot.png` (15:06:56): зелёная плашка читается
  «НАЧАТЬ ЗАНОВО / ЖМИ ЗЕЛЁНУЮ», причина + некролог на месте. Сам экран финала под
  эталон ещё не перекладывался — это не этот инкремент.
- **(m) suite HEADLESS (редактор life-choices закрыт):**
  `./bin/studio-go suite --project life-choices` → **`=== suite GREEN (exit 0) ===`**,
  `SUITE EXIT: 0`, свежий каталог `.studio/runs/suite-20260731-150723` (старт 15:07:23,
  свежее последней правки исходников 15:05:52), EditMode **243/243** + PlayMode
  **80/80** = **323/323**, 0 failed / 0 skipped.
- **(n) итог по файлам:** `Scripts/GameDriver.cs` (ввод, тексты, опенер, токены),
  `Art/Resources/Sprites/opener-logo-v2.png` + `.meta` (новые),
  `Tests/PlayMode/ControlLanguageTests.cs` (новый), `Tests/PlayMode/ScreensConformanceTests.cs`,
  `Tests/PlayMode/ArcadeExitTests.cs`, `Tests/PlayMode/ArcadeScreenshotTests.cs`.
  `Game.cs` / `Packages` / `ProjectSettings` не трогались. Ничего не коммичено.

- **(o) минорки после ПРИНЯТОГО гейта (оба скептика) — 4 правки, 2 кода + 2 текст/цвет:**
  1. **Гард no-dev-key усилен** (`ControlLanguageTests`). Было: подстроки
     `Enter/ПРОБЕЛ/Space/Numpad`. Стало: regex-набор + `↑`, `↓`, `стрелк` (кириллица) и
     ОДИНОЧНАЯ латинская `E` как имя клавиши —
     `(?<![0-9A-Za-z\p{IsCyrillic}_])[Ee](?![0-9A-Za-z\p{IsCyrillic}_])`: ловит «жми E»,
     « E », «(E)», не ловит внутрислов и не трогает кириллическую «Е» (другой кодпоинт).
     Ложных срабатываний на текущем контенте нет (проверено скриптом по всем строковым
     литералам `Scripts/*.cs` + пяти игровым колонкам `scenes.csv`: единственный хит —
     `"RingCenter"`, имя GameObject, а гард читает `Text.text`, не имена объектов).
     Сбор строк расширен с полей `GameDriver` на **любую форму** статического поля
     (`string`, `string[]`, словарь со строковыми/массивными значениями) у трёх
     контент-типов: `GameDriver` + `HostContent` + `Necrolog`, плюс **весь колодный CSV**
     (`CardLoader.ParseAll`: вопрос карточки, реплики Ведущего, обе строки некролога).
     ⚠ Уточнение к (g): утверждение «пул `HostContent` уже покрыт проверкой всех `Text`
     в иерархии» **неверно** — рендер-свип видит только ту ОДНУ реплику, что сейчас в
     пузыре; остальные ~36 строк пула были невидимы. Теперь пул покрыт явно (тест это и
     утверждает: `constants.Any(Where.StartsWith("HostContent.Pool"))`), а комментарий
     теста переписан по факту.
  2. **Устаревшие комментарии приведены к канону 99fab3c:** `GameDriver.OnInput` (док
     «Enter … sole key» → зелёная = физический confirm на опенере/хинте/финале, Enter =
     СКРЫТАЯ dev-эмуляция), ветка туториала («hints dismiss on Enter ONLY» → на событие
     CONFIRM, т.е. GREEN на кабинете), заодно ветка крутилки («Space is the money crank»
     → крутилка; confirm = GREEN); `ArcadeInputSource` шапка — Red больше не «finale
     restart», а «answer-only, на финале ИНЕРТНА», Green — «опенер/хинт/ФИНАЛ = confirm».
  3. **Зелёный финала = токен.** `AgainPlate` красился НИКАК: спрайт `plate-yes` несёт
     свою арт-зелень #5CBF5F, а uGUI-тинт только УМНОЖАЕТ — до #05CE51 (ярче по G)
     дотянуться тинтом физически нельзя. Плашка пересобрана как CTA опенера: кант
     `OnBarTrack(Ink)` + `bar-track` с `OnBarTrack(GoGreen)`, инсет текста 68→34 (у
     `bar-track` видимый пилл ~16 px против 55 у `plate-yes`). Замер на кадрах: финал
     **(0,205,81)** = опенер **(0,205,81)** — один в один. Тест: `AssertTokenGreen`
     (ScreensConformanceTests) считает РЕНДЕР-цвет (`BarTrackFillToken` × `Image.color`,
     новые публичные сиды `GoGreenToken`/`BarTrackFillToken`) и требует токен ±1/255 — и
     на опенерной, и на финальной CTA; перепись спрайтов финала обновлена (4× `bar-track`).
  4. **Двойное многоточие в некрологе.** Шов: `IntroLine` кончается «…», `ParentsLine`
     начинается «…», склейка была голым `Append(' ')`. Починен ШОВ, не контент:
     `Necrolog.Glue(prev, next)` — если предыдущий фрагмент уже кончается терминатором
     («…» или «.»), ведущее многоточие следующего («…» и ASCII «...») схлопывается, так
     что остаётся ровно один знак; `ComposeStory` идёт через него. CSV и константы не
     тронуты (в CSV, к слову, вообще нет строк, начинающихся с «…» — единственный
     источник шва это фиксированная `ParentsLine`). Юнит-тесты (EditMode):
     `Glue_CollapsesDoubleEllipsisAtTheSeam` (5 кейсов, включая «…»+«…» → один) и
     `ComposedStory_HasNoDoubleEllipsis`.
- **(p) suite HEADLESS после минорок (редактор life-choices закрыт):**
  `./bin/studio-go suite --project life-choices` → **`=== suite GREEN (exit 0) ===`**,
  честный `SUITE EXIT: 0`, свежий каталог `.studio/runs/suite-20260731-154220`
  (старт 15:42:20, свежее последней правки исходников), EditMode **245/245** (+2 новых
  некролог-теста) + PlayMode **80/80** = **325/325**, 0 failed / 0 skipped.
- **(q) кадр финала перегенерирован.** `LIFECHOICES_SHOT_POSE=finale` →
  `…/42c90134-…/scratchpad/inc-finale-shot.png` (15:44, `magenta=0`). Смотрел глазами:
  CTA — насыщенная токен-зелёная с тёмным кантом, читается «НАЧАТЬ ЗАНОВО / ЖМИ
  ЗЕЛЁНУЮ»; некролог открывается «Но не переживайте! Ведь вы… родились у прекрасных
  родителей.» — одно многоточие, шва не видно.
- **(r) итог по файлам (минорки):** `Scripts/GameDriver.cs`, `Scripts/Necrolog.cs`,
  `Scripts/ArcadeInputSource.cs`, `Tests/PlayMode/ControlLanguageTests.cs`,
  `Tests/PlayMode/ScreensConformanceTests.cs`, `Tests/EditMode/NecrologTests.cs`.
  `Game.cs` / `Packages` / `ProjectSettings` не трогались. Ничего не коммичено.

## 5. Skeptic verdict

- **Maintainer (глаз):** опенер против эталона — логотип/рамка/лампочки/текст/CTA как на
  макете; финал — токен-зелёная CTA, некролог без шва.
- **Code (Codex):** ПРИНЯТЬ (блокеров нет; ввод по всем веткам корректен: депрессия-ловля
  цела, блиц/импульс не задеты, крутилка не подтверждает, опенер строится один раз).
  MINOR (гард без «E»/стрелок; только static-поля) + NIT (устаревшие комменты) — закрыты:
  regex-гард с покрытием всего авторского контента (deck+Pool+Necrolog; вскрыто и
  починено ложное «пул покрыт» из прошлого раунда), комменты приведены к канону 99fab3c.
- **Design (зрячий):** ГЕЙТ ПРОЙДЕН — вся авторская работа инкремента ≤6 px к эталону,
  вырезка логотипа чистая (0 ореолов), переносы канонические, CTA токен. Мелочи (зелёный
  финала #5CBF5F → токен; шов «… …» в некрологе) — закрыты. Вынесено основательнице (не
  блокеры): фон опенера (жёлто-синий с залом на эталоне vs единый синий арт-пака — нужного
  ассета в паке нет) и композиция «CTA внутри плашки» (текст правил −18% от эталона).
- **Гейт headless:** финальный suite GREEN (exit 0) — **EditMode 245/245 + PlayMode 80/80
  = 325/325**, прогон `.studio/runs/suite-20260731-154220`.

## 6. Playtest verdict

`Playtest ok:` —
`Landed:` bd2be42 (2026-07-31)
