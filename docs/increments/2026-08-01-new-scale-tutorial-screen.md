# Increment: экран появления новой шкалы (6/7)

<!-- ГЕЙМПЛЕЙ+ВИЗУАЛ. Программа встречи 2026-07-29. Baseline: 11e8e81. -->

## 1. Пересказ дизайна

**§2 (meeting-revisions) + build-spec §D.** Эталон «Экран - появление новой шкалы.png».
Модальный туториал на открытии каждой шкалы (`OPEN:*`), поверх приглушённого геймплея:

1. **Новая шкала — КРУПНО** у своего HUD-места (увеличенная копия виджета).
2. **Окно-рассказ** — облачко Ведущего (`host-comment-v2`, верх-право ≈1010,90,730,290).
3. **Окно-задача** — кремовая плашка на подложке `ui_warning_v2.png` (центр ≈430,360,1130,460;
   НЕ 9-slice — язычки/звёзды, asset-map §5).

**Тексты — КАНОН host-content.md §4** (выбор основательницы): энергия «Ого! Что это?..» /
«Используй датчик высоты — дыши…» (ед.ч.!); отношения вар.1 «ЧУВСТВА!»; деньги вар.2
«Денежки любят…»; ребёнок вар.1 «Теперь вас трое!».

**Поведение (жёстко, §2):** пока экран активен — таймер ответа и дренажи НА ПАУЗЕ;
окно НЕ уходит по кнопке — только по ВЫПОЛНЕНИЮ условия:
| Шкала | Условие закрытия |
|---|---|
| Деньги (18) | ≥1 принятый тик крутилки |
| Отношения (20) | маркер в механической зоне 40–75 и удержать ~1.5 с |
| Энергия (25) | поднять энергию >40% дыханием и удержать ~1.5 с (§Q-controls: один рычаг) |
| Ребёнок (свадьба+2) | поднять один звонок («!») |
После выполнения — окно уходит (фейд 0.2 с), **StarBurst()**, пауза снимается.

Существующие S5-подсказки открытия шкал (текстовые хинты «ПОНЯТНО») этим ЗАМЕНЯЮТСЯ
для четырёх OPEN-шкал. Механика Game (что и когда открывается) не меняется; пауза —
существующий механизм подсказок (Game.Paused), продлённый до выполнения условия.

## 2. Done contract

| # | Class | Criterion |
|---|-------|-----------|
| 1 | boots | Старт ок. |
| 2 | player path | На каждом OPEN: затемнение + крупная шкала + рассказ + задача (канон-тексты); выполнение условия → звёзды → игра продолжается. |
| 3 | interaction path | Условия всех 4 шкал работают через реальные контролы (крутилка/рычаг/дыхание/«!»); окно нельзя закрыть кнопками-ответами. |
| 4 | failure/restart path | Пока окно активно — таймер/дренажи стоят (шкалы не умирают под туториалом); рестарт из туториала чист. |
| 5 | observability | Задача говорит, ЧТО делать (канон-текст); прогресс удержания виден (реши как: заполнение/таймер — задокументируй). |
| 6 | design | Против эталона: композиция трёх окон совпадает (±10 боксы), затемнение ~40% INK, крупная шкала у своего места. |
| 7 | regressions + Слой-2 | Тесты: пауза под окном (шкалы не тикают), незакрываемость ответами, условие каждой шкалы (реальный ввод → закрытие; невыполнение → стоит), звёзды на выходе, старые S5-хинты для OPEN сняты. Suite HEADLESS зелёный. |

## 3. Approved by founder

`Approved by founder:` 2026-07-29 — §2 спека встречи; тексты §4 host-content (её выбор);
энергия = один рычаг-дыхание (Q-controls закрыт ею же).

## 4. Checkpoint log

- **(a) start:** владелец собрал инкремент целиком: `Game.PausedInputsLive` (пауза, под которой живы
  ТОЛЬКО контролы четырёх шкал + удерживаемая ось балансира, `TickModalBalancer`), оверлей
  `BuildNewScaleOverlay` (затемнение 40 % INK → `task-plate-v2` Simple → текст задачи → полоска
  удержания → зеркальное `host-comment-v2` → текст рассказа → слот крупной шкалы), «одалживание»
  живого HUD-виджета (`BorrowBigWidget`/`ReturnBigWidget`), условия выхода на реальном вводе,
  фейд 0.2 с → `StarBurst()` → снятие паузы. Старые S5-хинты четырёх OPEN сняты (health-хинт
  остался — он не из этой четвёрки). Канон-тексты §4 host-content вшиты константами.
- **(b) взвод удержания:** решение владельца — удержание считается только ПОСЛЕ первого принятого
  ввода по шкале (`_nsArmed`). Энергия на открытии 100 %, отношения 55 % — обе уже «в режиме», без
  взвода экран уходил бы сам через 1.5 с, ничего не потребовав (та самая «шкала под пассивного
  игрока»). Прогресс удержания (done-contract §5) = полоска GREEN у нижнего края кремового поля,
  видима у энергии/отношений и ТОЛЬКО после взвода (`ReflectNewScaleHold`).
- **(c) остановка/восстановление:** владелец остановлен на шаге «снять 4 кадра модалок»; дерево
  восстановлено из git stash. Сессия-финишёр: аудит + добивка. Трёх тестовых файлов не дублируется —
  `NewScaleTut.cs` это ОБЩИЙ хелпер (`Clear`/`ClearAny`/`ClearArcade`), его зовут 14 PlayMode-файлов;
  огрызков не найдено, ничего не удалено. Позы харнесса `tutmoney/tutrel/tutenergy/tutchild` в
  `ArcadeScreenshotTests` владелец уже завёл.
- **(d) suite после восстановления:** прогнан ЗАНОВО целиком, HEADLESS, редактор life-choices закрыт.
  EditMode 273/273, PlayMode 125/125, exit 0/0 — GREEN.
  `.studio/runs/suite-20260805-123204/`.
- **(e) кадры + сверка с эталоном (инструментально, оба 1920×1080, пересчёт не нужен):** сняты все
  четыре модалки. **Два текстовых окна ложатся в допуск, крупная шкала — НЕТ.**
  | Элемент (энергия) | Эталон | Кадр | Δ центра |
  |---|---|---|---|
  | кремовое поле окна-задачи | x 386…1626, y 343…824 (1241×482), ц. (1006.0, 583.5) | x 393…1625, y 341…820 (1233×480), ц. (1009.0, 580.5) | (+3.0, −3.0) ✅ |
  | кремовое поле облачка | x 1014…1748, y 106…343 (735×238), ц. (1381.0, 224.5) | x 1014…1746, y 107…343 (733×237), ц. (1380.0, 225.0) | (−1.0, +0.5) ✅ |
  | крупная батарея (внутр. кромка обводки) | x 207…412, y 145…546 (206×402), ц. (309.5, 345.5) | x 123…289, y 270…595 (167×326), ц. (206.0, 432.5) | (−103.5, +87.0), размер 81 % ❌ |

  Затемнение сверено численно: 40 % INK (#0B0F1A) поверх фона эталона в ЛИНЕЙНОМ пространстве даёт
  sRGB (20, 78, 197), в кадре измерено (23, 80, 195) — сходится, `NewScaleDimAlpha = 0.40f` верен.
- **(f) ОТКРЫТЫЙ ДЕФЕКТ композиции (на дизайн-гейт, кодом НЕ трогал):** промах не только у энергии —
  `BorrowBigWidget` ставит виджет по `to + k·(from_факт − from_конст)`, т.е. промах = ошибка в
  константе `BigXxxFrom`, помноженная на масштаб. Следствия на кадрах:
  энергия — сдвиг (−103.5, +87) и 81 % размера против эталона (комментарий в коде «энергия сверена
  с эталоном инструментально» по факту НЕ подтверждён рендером); деньги — банка ОБРЕЗАНА правым
  краем экрана (контент упирается в столбец 1919, строки 723…887) и налезает на плашку возраста;
  ребёнок — трубка ОБРЕЗАНА левым краем (столбец 0, строки 270…852); отношения — единственная без
  обрезки. Требуется перекалибровка `BigEnergyFrom/BigMoneyFrom/BigChildFrom` + масштабов; это
  дизайнерское решение, поэтому оставлено гейту, а не правлено финишёром.

- **(g) дефект композиции ПОЧИНЕН (мандат координатора).** Корень оказался не в константах, а в
  раскладке: `BorrowBigWidget` замораживал сдвиг группы в ПИКСЕЛЯХ канваса на момент «одалживания», а
  дети группы сидят на ДОЛЯХ канваса (`AnchorPx`) и пересчитываются на каждом layout-проходе. Стоило
  канвасу поменять размер уже ПОСЛЕ показа модалки (харнесс скриншотов пиннит его с ≈1664×1248 на
  1920×1080), как дети уезжали, а замороженный сдвиг — нет. Проверено численно: предсказание модели дало
  центр батареи (208.1, 420.0) при измеренных (206.5, 417.5).
  Сделано:
  1. Сдвиг теперь идёт **ЯКОРЯМИ** (`PlaceBigWidget`) — в долях канваса, поэтому переживает любой ресайз;
     `ReturnBigWidget` возвращает якоря/отступы, а не одну `anchoredPosition`.
  2. Раскладка задаётся **БОКСАМИ**: `BigScaleSrc` (бокс нарисованного виджета в HUD, замерен по кадру
     `hud-phonering` при канвасе ровно 1920×1080) → `BigScaleDst` (целевой бокс). Масштаб ВЫВОДИТСЯ
     (k = dst.w / src.w), поэтому промах источника больше не умножается на масштаб.
  3. Разведены два разных бокса батареи, которые прошлый владелец путал: **внутренняя кромка обводки**
     (её и меряют с эталона: x 207…412, y 145…546) и **весь рисунок** (alpha-tight: x 180…435, y 81…575).
     Тест `Composition_ThreeWindows…` строил бокс по рисунку, а сверял с кромкой — и потому «подтверждал»
     раскладку, промахивавшуюся на ~104 px. Константа исправлена на одноимённую фигуру.

  Целевые боксы (дизайн-пиксели 1920×1080, cx / cy-от-верха / w / h):
  | Шкала | Src (в HUD) | Dst (на модалке) | k | Где |
  |---|---|---|---|---|
  | Энергия | 183.5, 166.5, 114×218 | 309.5, 326.4, 255.6×488.8 | ×2.24 | слева, ПО ЭТАЛОНУ |
  | Отношения | 674, 100.5, 523×130 | 515, 178, 993.7×247.0 | ×1.90 | верх-центр-лево |
  | Деньги | 1750, 127.5, 173×244 | 1764.5, 835, 273.3×385.5 | ×1.58 | правый столбец |
  | Ребёнок | 144, 540, 411.3×444.2 | 234, 540, 431.9×466.4 | ×1.05 | левый край, ЦЕЛИКОМ в кадре |

  Два ограничения мандата оказались взаимно невыполнимы, разрешено так (обосновано арифметикой в
  комментарии к `NewScaleBigWidgetLayoutTests`):
  • «не пересекать кремовые поля» соблюдено для ДЕНЕГ и ОТНОШЕНИЙ. Для ЭНЕРГИИ его нарушает сам эталон
    (батарея перекрывает левые лучи плашки — так нарисовано); для РЕБЁНКА оно требует ширины ≤377 px
    против 411 px его же HUD-размера, то есть УМЕНЬШЕНИЯ. Для этих двух граница — ТЕКСТ, а не плашка.
  • «~×2» достижимо у энергии и отношений; у денег правый верх занят облачком и бейджем (максимум ×1.58
    в свободном правом столбце), у трубки текстовое поле задачи начинается на x=452 → максимум ×1.06.
    Порог `> 1.2` в существующем тесте ослаблен ТОЛЬКО для ребёнка и только до `> 1.0`, зато добавлена
    жёсткая сверка `k == BigScaleFactor(kind)`.

  Тесты (добавка, не ослабление): `NewScaleBigWidgetLayoutTests` (EditMode, 16 проверок) — целиком в
  кадре с полем ≥16 px, мимо текста обоих окон и бейджа, крупнее HUD и без сплющивания, энергия против
  эталонного бокса ±10; `NewScaleBigWidgetPlacementTests` (PlayMode, 9) — виджет реально приезжает в
  целевой бокс (через настоящие матрицы Unity) и **остаётся там после РЕСАЙЗА КАНВАСА** — регрессия
  ровно на корень дефекта, плюс возврат виджета в HUD.
- **(h) сверка после починки.** Suite HEADLESS заново: EditMode **289/289**, PlayMode **134/134**,
  exit 0/0 — GREEN, `.studio/runs/suite-20260805-134209/`. Кадры пересняты все четыре.
  Энергия против эталона (обе картинки 1920×1080, замер по внутренней кромке обводки):
  | Кромка | Эталон | Кадр | Δ |
  |---|---|---|---|
  | левая | 207 | 208 | +1 |
  | правая | 412 | 412 | 0 |
  | верхняя | 145 | 145 | 0 |
  | нижняя | 546 | 546 | 0 |

  Кремовые поля не сдвинулись: задача — эталон x 386…1626 / y 343…824 против кадра x 393…1625 / y 341…820;
  облачко — эталон x 1014…1748 / y 106…343 против кадра x 1014…1746 / y 107…343. Обрезки контента о край
  кадра не осталось НИ НА ОДНОЙ из четырёх модалок (проба по столбцам 0 и 1919: 0 перепадов везде;
  до починки было 27 у денег справа и 27 у ребёнка слева).
- **(i) находки код-скептика починены (BLOCKER + MAJOR).** Обе — в поведении §2, мандат координатора.
  Правки только в `GameDriver.cs`; `Game.cs` трогать не пришлось — обе дырки оказались на драйверной
  стороне воронки ввода. Визуал НЕ менялся, кадры не переснимались.

  1. **BLOCKER — ферма денег под чужой модалкой.** `NewScaleInput` пропускал в `Game` ВСЕ четыре
     scale-контрола, а не только контрол объясняемой шкалы. Под модалкой время стоит (возраст, стоимость
     жизни, все дренажи), поэтому любой из четырёх экранов работал бесконечной крутилкой: получил экран
     энергии — крути деньги сколько влезет в мире, где за них ничего не списывается; и симметрично —
     чужим дыханием можно было поднять энергию с 10 до 100 под детским экраном. Ограничения по времени
     нет: окно ждёт условия сколько угодно. Фикс — фильтр по шкале в начале каждой ветки
     `NewScaleInput`: `_nsWhich != <шкала> → return`. Чужие scale-вводы теперь инертны ровно как ДА/НЕТ.
     Взвод `_nsArmed` внутри веток стал безусловным (ветка и так своя).
  2. **MAJOR — детская модалка под ВЫГОРАНИЕМ вставала невыполнимой.** Условие экрана — поднять звонок,
     а под выгоранием это запрещено дважды: драйвер прячет трубку целиком (`ReflectChildPhone`, плашка S7 —
     полноэкранный захват), и `Game.ChildCallFrozen` морозит и окно, и само нажатие. Пауза держится,
     условие недостижимо, ответы инертны — прогон мёртв. Развилка есть ТОЛЬКО у ребёнка: он открывается на
     «свадьба+2», то есть может прийти после энергии (25), а выгорание требует открытой энергии ≤10 %;
     деньги (18) и отношения (20) открываются раньше энергии, так что при их OPEN выгорания не бывает.
     Фикс — **отложка, а не пропуск**: `ShowNewScale` при `Child && Burnout` НЕ помечает `_nsSeen`, а ставит
     `_nsPending`; `PumpPendingNewScale` (зовётся из `TickNewScale`, то есть и из `Update`, и из `DebugTick`)
     ретраит каждый такт и поднимает экран, как только выгорание снято (ретрай, а не фронт: сверху может
     стоять S5-подсказка). Отложенный OPEN снимается при уходе из `Playing` и на свежей жизни.
     Seam для тестов — `GameDriver.NewScalePending`.
  3. **Вторая половина развилки доказана, а не предположена:** выгорание НЕ МОЖЕТ начаться под уже открытой
     модалкой (`BurnoutCannotStart_UnderAnOpenChildModal…`) — под паузой `Tick` не доходит до дренажа
     энергии, а единственный оставшийся путь пересчёта (вдох) энергию только ПОДНИМАЕТ и на чужом экране
     вдобавок отфильтрован п.1. Поэтому обработка выгорания в СЕРЕДИНЕ экрана не нужна.

  Тесты (добавка, не ослабление), `NewScaleTutorialTests` 9 → 12:
  `UnderAModal_ForeignScaleControls_AreInert_NoCrankFarming` (крест-накрест на трёх экранах: энергия ←
  крутилка+рычаг, отношения ← крутилка, ребёнок ← крутилка+рычаг; тут же — что СВОЙ контрол жив, иначе
  «фикс» мог бы просто убить весь ввод), `ChildModal_UnderBurnout_IsDeferred_NotRaisedUnwinnable`,
  `BurnoutCannotStart_UnderAnOpenChildModal_SoTheScreenStaysWinnable`. Ребёнок в первых двух заводится
  «поздней» колодой (`LateChildDeck`, MD02 на 26): обычная открывает его на 21, когда возраст не догнал
  даже 18 и деньги закрыты — проверка «крутилка не крутит» была бы ПУСТОЙ.

  **MUTATION-PROOF** (точечные прогоны `NewScaleTutorialTests`, тот же редактор, что у suite):
  | Прогон | Итог |
  |---|---|
  | baseline (с фиксами) | 12/12 GREEN, exit 0 |
  | мутация 1: `NewScaleInput` откачен ДОСЛОВНО к отревьюенному виду | **RED 10/12** — `…NoCrankFarming`: «крутилка на ЧУЖОМ экране не приносит ни копейки» ожидал 0.8 ₽, получил **40.8 ₽**; `BurnoutCannotStart…`: энергия под детским экраном ожидалась 10, получилось **100** |
  | мутация 2: снята отложка (блок `Child && Burnout` в `ShowNewScale`) | **RED 11/12** — `ChildModal_UnderBurnout…`: «невыполнимый экран под выгоранием НЕ поднимается» ожидал False, получил True |
  | фиксы возвращены (файл побайтово сверен) | 12/12 GREEN, exit 0 |

  **Suite HEADLESS заново** (редактор life-choices закрыт): EditMode **289/289**, PlayMode **137/137**,
  `=== suite GREEN (exit 0) ===`, `.studio/runs/suite-20260805-140411/`. Полоска удержания и StarBurst на
  выходе не сломаны — соответствующие тесты (`Energy_NeedsAValidBreath…`, `Money_ClosesOnly…`,
  `Relations_TheLever…`, `Child_RingsUnderTheModal…`) зелёные без правок.

## 5. Skeptic verdict

- **Maintainer (глаз):** все четыре модалки; финально — комикс-композиция целостна,
  канон-тексты читаются, крупные виджеты у своих мест. Также вскрыт и исправлен
  финишёром скрытый дефект владельца: BorrowBigWidget замораживал сдвиг в пикселях
  канваса → ресайз уводил виджеты (банка/трубка резались краями, батарея −104 px);
  переведено на якоря в долях + регрессия ровно на ресайз.
- **Code (Codex):** ВЕРНУТЬ → закрыто: (BLOCKER) фарм денег под чужой модалкой —
  под модалкой жив только контрол открываемой шкалы (фильтр вскрыл и симметричную
  дырку: чужое дыхание качало энергию); (MAJOR) child-модалка под выгоранием
  невыполнима — отложка _nsPending с ретраем до снятия выгорания; (б-ветка
  «выгорание не может начаться под модалкой» доказана тестом. Все mutation-proof
  (дословный откат отревьюенного кода → красный).
- **Design (зрячий):** ГЕЙТ ПРОЙДЕН — энергия против эталона ±5 px по всем окнам,
  остальные три по той же композиционной логике, затемнение поднимает ровно один
  виджет, глифы в пилюлях. Косметика на решение основательницы (в долг полировки):
  фактический дим −20% против «40% INK» формулировки; затемнённый купол сверху
  читается «культёй»; ось банка↔бейдж в деньгах; разнобой тире «–»/«—».
- **Гейт headless:** финальный suite GREEN (exit 0) — **EditMode 289/289 + PlayMode
  137/137 = 426/426**, прогон `.studio/runs/suite-20260805-140411`.

## 6. Playtest verdict

`Playtest ok:` —
`Landed:` 12865cb (2026-08-05)
