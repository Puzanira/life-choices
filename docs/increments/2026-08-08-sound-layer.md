# Increment: Звуковой слой — внедрение манифеста дизайн-сессии

<!-- ЗВУК/ТЕХНИКА. Посадка дизайн-сессии «Звук» (завершена, коммиты 3345745 +
     1b53a24). Источники: docs/assets/audio-picks/manifest.md (событие → файл →
     источник/лицензия), docs/design-tasks/sound-direction.md (направление),
     docs/design-tasks/2026-08-07-sound-events-inventory.md (опись с частотами).
     НОЧНОЙ ПРОГОН: ушной гейт — ТОЛЬКО основательница утром; ночью Codex +
     структурные тесты + suite. Громкости/миксы — черновые, помечать тюнимыми. -->

## 1. Пересказ дизайна

В игре звука нет вообще. Дизайн-сессия отобрала файлы (все CC0) и решила:
зал/аплодисменты СНЯТЫ, эмбиент и вехи возраста — намеренно без звука,
калимба — голос игры (ответы, «алло», второй шанс, лечение, расставание),
Ведущий — шесть пиццикато по тонам реплик, финал — три конца (скрипка/казу/
обрыв пластинки), энергия — один жест зарядки. Главный художественный приём —
ДЕПРЕССИЯ: весь микс уходит «в вату» (лоу-пасс), возврат по ступеням попаданий.

Внедрение:

1. **Аудио-слой в драйвере**: единая точка проигрывания (SFX + луп-каналы),
   импорт файлов из docs/assets/audio-picks/ в Assets (структура по группам
   манифеста), маппинг событие → клип по манифесту, «намеренно без звука» —
   список-исключение, НЕ дыра.
2. **Разводка событий**: подписки на существующие события Game/GameDriver
   (ответы/раздача/купол/BLOCK$/крутилка/монета/тревоги/энергия-жест/
   выгорание/отношения/расставание/второй шанс/здоровье/телефон/салют/
   туториалы/блиц/импульс/депрессия/опенер/финал-по-типу-конца/рестарт).
   Каждое событие манифеста должно иметь подписку или явную пометку «нет
   события в коде» в чекпоинт-логе (не выдумывать событий).
3. **Фильтр депрессии**: лоу-пасс на весь микс по спеке манифеста; вход —
   «в вату», каждое попадание пульса возвращает ступень (5 ступеней), выход —
   «мир снова включили». Депрессионный одинокий удар сердца — вне фильтра.
4. **Лицензии**: атрибуции уже в манифесте; в билд файлы кладутся как есть,
   лицензии не переоформлять.

## 2. Done contract

| # | Class | Criterion |
|---|-------|-----------|
| 1 | boots | Старт ок; все клипы манифеста загружаются (нет битых путей/форматов); нулевой клип не валит игру. |
| 2 | player path | Обычный забег озвучен: раздача/ответы/купол/крутилка/монеты/тревоги/салют слышны (структурно: события дёргают проигрыватель с верным клипом). |
| 3 | interaction path | Спецрежимы: блиц/импульс/выгорание/телефон/финал-по-типу-конца разведены по своим клипам; депрессия глушит микс и возвращает по ступеням; лупы бесшовны (структурно: loop-флаг). |
| 4 | failure/restart path | Рестарт сбрасывает фильтр/лупы/каналы (нет «застрявшей ваты» после смерти в депрессии); пауза туториалов не копит очередь звуков. |
| 5 | observability | Каждая строка манифеста → подписка или явная пометка; «намеренно без звука» — тест-список; громкости — константы с пометкой ТЮНИМО (ушной гейт утром). |
| 6 | design | — (ушной гейт основательницы утром; ночью не судим художественно). |
| 7 | regressions + Слой-2 | Структурные тесты: маппинг манифеста полон, фильтр-ступени 5, сброс на рестарте; существующие тесты не тронуты по смыслу; suite HEADLESS ×2 зелёный. |

## 3. Approved by founder

`Approved by founder:` 2026-08-08 — манифест собран её дизайн-сессией
(«Звук игры собран целиком»), внедрение отдельным инкрементом — по плану
описи; ночной прогон «реализовать максимально» — явное слово.

## 4. Checkpoint log

- **(a) start:** прочитан контракт + манифест + дирекция + опись. Снята полная карта точек
  срабатывания по `GameDriver.cs` (5492 строки): 37 моментов, из них 6 без события в коде.
- **(b) импорт:** 43 файла манифеста скопированы в НОВЫЙ Resources-корень
  `Assets/_Project/Audio/Resources/Audio/<группа>/` (структура групп манифеста сохранена).
  6 прослушек НЕ импортированы (`--3voices`, `stingers--…DEMO-6`, `DEMO--0/1/2`,
  `theme--…DEMO-in-depression`) — манифест прямо пишет «в сборку не идёт». Настройки импорта
  ставит редакторный `AudioImportSettings` (AssetPostprocessor) по правилу из рантайма
  `AudioCatalog.StreamingFor`: короткие SFX — DecompressOnLoad + PCM, лупы — Streaming + Vorbis,
  всё `forceToMono` (дизайн-сессия свела файлы в моно 44.1). Правило живёт в рантайме нарочно —
  так его проверяет EditMode-тест и .meta не разъезжаются с манифестом молча.
- **(c) архитектура:** три новых файла рантайма.
  `SoundCatalog.cs` — `enum SoundEvent` (45 событий = строки манифеста), `SoundDef`
  (файл/шина/канал/громкость/питч/джиттер), `AudioCatalog` (маппинг + список «намеренно без
  звука» + мапперы `ForHostTone`/`ForAlarm`), `FinaleSound` (строка причины → тип конца).
  `DepressionMix.cs` — чистая таблица ступеней ваты, без сцены.
  `AudioLayer.cs` — MonoBehaviour на дочернем объекте драйвера: SFX-пул 12 голосов (фильтруется),
  3 именованных луп-канала Music/PhoneRing/Impulse (фильтруются), нефильтруемый пул 2 голоса
  РОВНО под удар сердца депрессии. Очереди нет по устройству: `Play` либо звучит сейчас, либо не
  звучит вовсе. Прямых источников звука в драйвере нет — тест это стережёт.
- **(d) фильтр депрессии:** лоу-пасс висит НА КАЖДОМ фильтруемом источнике (`DepressionMix.Cascades`
  = 2 каскада по 2 полюса, как в спеке), нефильтруемый пул их не несёт — поэтому пульс остаётся
  «наверху нетронутым», а музыка садится вместе со всем миксом. Вход — ступень 0 (500 Гц / −9 дБ)
  разом; каждое попадание → `SetDepressionHits`; пятое — фильтр снят. Ослабление в дБ едет
  множителем громкости, срез — частотой. `ResetAll()` на рестарте/финале снимает вату и глушит
  лупы: смерть В ДЕПРЕССИИ больше не оставляет «застрявшую вату».
- **(e) решения, которые стоит знать основательнице/скептику:**
  1. `HostTone.Debt` появился в коде ПОЗЖЕ манифеста (шесть пиццикато на шесть тонов пула). Своего
     файла ему не выдумывал — долг берёт голос «осторожность». Захочет свой стингер — это строка
     манифеста, не правка кода.
  2. Входные экраны спецрежимов (блиц/депрессия/выгорание/здоровье) «тушем» туториала НЕ звучат:
     у каждого свой голос из манифеста, иначе четыре разных экрана звучали бы одинаково. Туш
     остался за §D-модалками новых шкал (открытие + выполнение условия).
  3. Рингтон и «сирена» импульса сделаны ЛУПАМИ на длину состояния (окно звонка 5 с, раунд
     импульса), а не одиночными выстрелами: манифест просит «настойчивый» и «тикающая бомба».
  4. Тревоги звучат НА ФРОНТЕ, один раз на заход, а не пульсом 0.7 с — иначе прямое нарушение
     правила отбора №2 («ничего резкого, человек слушает это всё время»).
  5. `FinaleKind`: «…старость» → скрипка, «полное выгорание» → казу, ВСЁ остальное (фатальные
     карточки, «здоровье не выдержало», «неведомая дичь») → обрыв пластинки.
  6. В `Game.cs` добавлено ОДНО событие — `ChildBadParent` (три строки, по образцу соседнего
     `ChildCallMissed`): строка манифеста «Плохой родитель» иначе была бы ненаблюдаемой (счётчик
     приватный и обнуляется тем же кадром). Это единственная правка механики; поведение не тронуто.
  7. `ShowTutorial` (легаси S5) звук НЕ получил: продакшн-вызовов у него не осталось, дёргается
     только из debug-шва.
- **(f) ТЮНИМО:** все громкости — черновые константы в одном блоке `AudioCatalog`
  (`VolMusicBed` 0.35 · `VolFrequentTick` 0.28 · `VolFrequentSoft` 0.35 · `VolBlip` 0.40 ·
  `VolDome` 0.45 · `VolCard` 0.50 · `VolHostStinger` 0.50 · `VolPhonePickup` 0.50 · `VolEvent` 0.65 ·
  `VolAnswer` 0.70 · `VolReward` 0.70 · `VolBig` 0.85 · `VolLoud` 0.90), плюс `MasterVolume` на слое.
  Уровни между источниками манифестом НЕ выровнены — сведение за ушным гейтом.

### Полная таблица «строка манифеста → разводка»

Ни одной строки без адресата: 43 файла → 45 событий (дорожка тревог одна на три высоты),
2 строки — намеренная тишина.

| # | Строка манифеста | Событие | Где разведено |
|---|---|---|---|
| 1 | Музыка-тема | `MusicTheme` (луп) | `Start()` + `Refresh` (канал Music) |
| 2 | Эмбиент шоу | — | **НАМЕРЕННО БЕЗ ЗВУКА** |
| 3 | Раздача карточки | `CardDeal` | `OnCardChanged` |
| 4 | Ответ ДА | `AnswerYes` | `OnInput` (принятый ответ) + попадание блица |
| 5 | Ответ НЕТ | `AnswerNo` | `OnInput` + латч `Game.BlitzFails` (провал блица) |
| 6 | Таймаут | `Timeout` | `OnAnswerResolved` (`AnswerSide.Timeout`) |
| 7 | Купол: последняя секунда | `DomeLastSecond` | **латч** `TickAudioLatches` — события в коде нет |
| 8 | BLOCK$ | `BlockMoney` | `OnCardChanged` (карточка пришла заблокированной) |
| 9 | Тик крутилки | `CrankTick` | `OnInput` (крутилка) + `NewScaleInput` (модалка денег) |
| 10 | Монета в банку | `CoinJar` | `OnInput` + `NewScaleInput` |
| 11 | Тревоги денег/энергии/здоровья | `AlarmMoney`/`Energy`/`Health` | `ReflectAlarms`, фронт, через `ForAlarm` |
| 12 | Сцена зарядки | `EnergyCharge` | `NoteEnergyGesture` (`OnInput` + `NewScaleInput`) |
| 13 | Выгорание: вход | `BurnoutIn` | `OnBurnoutEntered` |
| 14 | Выгорание: выход | `BurnoutOut` | **латч** — Game гасит флаг молча, события нет |
| 15 | Вошёл в зелёную зону | `ZoneIn` | `ReflectAlarms`, спад тревоги Relations |
| 16 | Вышел из зоны | `ZoneOut` | `ReflectAlarms`, фронт тревоги Relations |
| 17 | РАССТАВАНИЕ | `Breakup` | `OnRelationshipBrokeUp` |
| 18 | Второй шанс (MD06) | `SecondChance` | `OnRelationshipsOpened` при ПОВТОРНОМ открытии |
| 19 | Открытие здоровья (30) | `HealthOpen` | `OnHealthOpened` |
| 20 | Лечение выбором | `Heal` | `OnAnswerResolved` → `HealsHealth` по Δ — события нет |
| 21 | Звонок: выезд трубки | `PhoneRing` (луп) | `ReflectChildPhone`, фронт/спад звонка |
| 22 | Поднял трубку | `PhonePickup` | `PressChildPhone` + `NewScaleInput` |
| 23 | Проспал | `PhoneMissed` | `OnChildCallMissed` |
| 24 | «Плохой родитель» | `BadParent` | `Game.ChildBadParent` — **событие добавлено** (см. (e)6) |
| 25 | StarBurst | `StarBurst` | `GameDriver.StarBurst()` — единственная точка салюта |
| 26–31 | Ведущий, 6 тонов | `Host*` | `OnAnswerResolved` → `ForHostTone(_voice.Classify)`; `OnDebtEntered` → `debt`→cautious |
| 32 | Туториал: открылся / выполнено | `Tutorial` | `ShowNewScale` + `CloseNewScale(reward:true)` |
| 33 | Блиц: старт | `BlitzStart` | `OnCrisisStarted` |
| 34 | Блиц: мысль | `BlitzThought` | `OnCrisisBlitzAdvanced` (до раннего возврата — звучат все 5) |
| 35 | Импульс (INVERT) | `Impulse` (луп) | `OnCrisisImpulseStarted`, снимается в `OnCrisisEnded` |
| 36 | Депрессия: вход | `DepressionEnter` | `OnDepressionStarted` + `Audio.EnterDepression()` |
| 37 | Депрессия: пульс | `DepressionPulse` | **латч**, шина `Unfiltered` — вне ваты |
| 38 | Депрессия: промах/замок | `DepressionMiss` | `OnInput` (ловля в депрессии, серость не упала) |
| 39 | Депрессия: выход | `DepressionExit` | `OnDepressionEnded` — **подписка добавлена** |
| 40 | Опенер «НАЧАТЬ ЖИЗНЬ» | `Opener` | `Refresh`, переход Opener→Playing |
| 41 | Вехи возраста | — | **НАМЕРЕННО БЕЗ ЗВУКА** |
| 42 | ФИНАЛ ×3 | `FinaleOldAge`/`Burnout`/`Fatal` | `Refresh` →Finale через `FinaleSound.EventFor(Game.Cause)` |
| 43 | «НАЧАТЬ ЗАНОВО» | `Restart` | `Refresh`, переход Finale→Opener |

«Попадание в депрессии» отдельного клипа НЕ получает намеренно: его звук — сама снимаемая ступень
ваты. «Зал/аплодисменты» — третья намеренная тишина, событий под неё нет нигде.

- **(g) ЧЕКПОИНТ ДЛЯ ОСНОВАТЕЛЬНИЦЫ (прогон вживую, 2026-08-08):** дерево компилируется,
  EditMode **453/453 зелёный** (417 базовых + 36 новых), в логе ноль `error CS`, ноль
  «audio clip not found», ноль предупреждений о каскадах. Полный suite и PlayMode-набор на этот
  момент ЕЩЁ НЕ ПРОГНАНЫ — прогон отложен, чтобы не держать редактор. TODO после её сессии:
  suite ×2 + PlayMode (два новых файла тестов: `AudioLayerTests`, `AudioDriverWiringTests`).
  Не проверено вживую: реально ли Unity вешает ДВА каскада лоу-пасса на источник (проверяет
  PlayMode-тест `FiltersHang_OnFilteredSourcesOnly`; если каскад один — «вата» будет мягче демо,
  слой это логирует предупреждением и продолжает работать).
- **(h) ДВА ДЕФЕКТА, НАЙДЕННЫХ ЖИВЫМ ПЛЕЙТЕСТОМ (2026-08-08).** Оба чинила основательница, моё —
  гарды. Оба класса headless не ловил, и это главный урок инкремента.
  1. **ТИШИНА: в сцене не было `AudioListener`.** Слой работал, клипы грузились, 453 EditMode-теста
     были зелёные — а игра молчала целиком: Unity без слушателя не издаёт ни звука и НЕ РУГАЕТСЯ.
     Гард: `Tests/PlayMode/SceneAudioTests.cs` — на ЗАГРУЖЕННОЙ боевой сцене ровно один включённый
     слушатель, он на камере, и драйвер там же поднимает слой со всеми клипами.
     Mutation-proof подтверждён: снял компонент из `.unity` (+ ссылку в `m_Component`) → оба гарда
     красные (`Expected: 1, But was: 0`), при этом **остальные 226 тестов остались зелёными** —
     ровно та слепота, из-за которой дефект и доехал до плейтеста. Сцена восстановлена побайтно.
  2. **Стингер Ведущего играл на 100 % ответов** при облачке в 30–40 %: пиццикато ложилось поверх
     калимбы на каждой карточке. Причина — безусловный `Play` в `OnAnswerResolved`; стингер это
     ГОЛОС ОБЛАЧКА, а не звук ответа. Починка (её): `Play` перенесён внутрь
     `if (!string.IsNullOrEmpty(line))`. Гард: `HostStinger_SoundsOnlyWhenTheBubbleActuallySpeaks` —
     покарточная сверка «облачко ⇔ стингер» на 39 карточках, плюс контроль
     `NamedHostLine_AlwaysSpeaks_AndAlwaysStings` (именная реплика показывается всегда → стингер
     обязан быть). Mutation-proof подтверждён: вернул безусловный `Play` → красное на ПЕРВОЙ же
     немой карточке. Инвариант не зависит от RNG пула, сид не трогаем.
  ⚠ Побочно: мой собственный тест `AnswerYes_..._PlusAHostStinger` ТРЕБОВАЛ стингер на каждый
  ответ — то есть кодировал баг. Её починка его честно уронила; требование снято, тест сузился до
  `AnswerYes_SpeaksTheKalimbaUp`. Ровно тот случай, о котором предупреждает память проекта
  «зелёный тест при сломанной игре».
- **(i) ВКУСОВОЕ ПРАВИЛО ОСНОВАТЕЛЬНИЦЫ НА БУДУЩЕЕ (не чинить сверх сделанного):** «по нажатию
  карточки — только звуки ответа». Сейчас на ответ, кроме калимбы, могут прийти `Heal` (карточка с
  плюсом к здоровью) и `CardDeal` (раздача следующей). Оба ОСТАВЛЕНЫ намеренно: sound-direction
  говорит «если после починки всё ещё густо — резать их», а густо это или нет — решает её УХО, а не
  тест. Следующей звуковой сессии: начинать с этого вопроса.
- **(j) ВЕРИФИКАЦИЯ (финальная).** `./bin/studio-go suite --project life-choices` ×2 подряд, оба
  «suite GREEN (exit 0)», exit снят отдельной строкой (`SUITE_A_EXIT=0` / `SUITE_B_EXIT=0`),
  каталоги свежие: `.studio/runs/suite-20260808-125950` и `.studio/runs/suite-20260808-130113`.
  Счёт: **EditMode 453/453** (417 базовых + 36 новых),
  **PlayMode 228/228** (202 базовых + 26 новых). Stale-локов не было, редактор всё это время закрыт.
  Один прогон (`suite-20260808-125114`) был КРАСНЫМ и это честно: флака дал МОЙ гард стингера —
  на ПОСЛЕДНЕЙ карточке колода кончается, `Refresh` стирает облачко раньше, чем тест его прочитает,
  и проба врала «пиццикато без облачка». Починен наблюдательный шов (последняя карточка не
  наблюдаема), не продакшн-код; после починки два зелёных прогона подряд.
- **(k) ⚠ РАСХОЖДЕНИЕ СО СПЕКОЙ, КОТОРОЕ ОБЯЗАНА УСЛЫШАТЬ ОСНОВАТЕЛЬНИЦА: каскадов 1, а не 2.**
  Спека просит «2 каскада по 2 полюса» (4 полюса, −24 дБ/окт). Прогон показал: **Unity не даёт
  повесить второй `AudioLowPassFilter` на тот же объект** — сколько ни добавляй, остаётся один.
  Значит «вата» сейчас 2-полюсная (−12 дБ/окт) и МЯГЧЕ того демо, по которому приём утверждали
  на слух. Частоты и −дБ по ступеням при этом ровно по таблице, вход/возврат/выход работают.
  Догадками не правил (подкрутить срез «на глаз» под чужой утверждённый эталон — не моя работа):
  выношу на ушной гейт, эталон для сверки лежит в `docs/assets/audio-picks/10-crisis/DEMO--1-cotton.mp3`.
  Если скажет «глуше» — честный путь один: AudioMixer-группа с двумя Lowpass-эффектами
  (фильтруемые источники в неё, пульс депрессии — мимо, как сейчас); это отдельная задача, потому
  что экспонирование параметров микшера headless не настраивается.
  Слой деградирует честно: логирует расхождение ОДИН раз на слой и работает на том, что дали.

- **(l) НАХОДКИ CODEX ЗАКРЫТЫ (2026-08-08, fix-сессия).** Все три с mutation-proof (сломал →
  красный с ожидаемым сообщением → вернул); плюс один дефект того же класса, найденный по дороге.

  1. **[MAJOR] Монета звенела на ОТВЕРГНУТУЮ крутилку** (`GameDriver.OnInput`, ветка `MoneyTick`).
     Условие смотрело на `_game.MoneyOpen`, а не на возврат `Game.HandleInput`: в депрессии и в
     кризисе механика ввод глушит (`Game.cs` §Playing), а банка всё равно звенела до 5 раз в
     секунду — ложный доход на слух поверх самой тихой сцены игры плюс забитый SFX-пул. Теперь и
     тик, и монета живут ВНУТРИ accepted-блока — та же семантика, что у панча плашек (r3) и
     §6-окна. Тот же фикс во второй точке — крутилка под §D-модалкой денег (`NewScaleInput`).
     ВИЗУАЛ падающей монеты намеренно НЕ тронут (он с r2, это не звуковая находка) — но под
     спецрежимами он тоже рисуется, и это вопрос к дизайну, а не к звуку.
  2. **[MAJOR] Рингтон звенел из-под кризиса.** `Game.Tick` уходит в кризис РАНЬШЕ, чем доходит до
     ребёнка, поэтому звонок остаётся `ChildFlashing`, `RenderCrisis` прячет трубку и уходит — а
     единственный `StopLoop(PhoneRing)` жил в `ReflectChildPhone`, которая под кризисом не
     выполняется. Латать вторым `StopLoop` в `RenderCrisis` значило бы заводить новую ветку на
     каждый новый способ спрятать виджет. Введена ЕДИНАЯ ТОЧКА ИСТИНЫ — `PhoneRingAudible` +
     `SyncPhoneRingLoop()`: луп звучит ровно пока объект трубки жив в иерархии (кризис, финал,
     рестарт, выход — все гасят именно его) И окно звонка открыто И оно реально крутится
     (`ChildCallFrozen` — глухая пауза его стопорит; «живая» пауза §D-модалки ребёнка не
     стопорит). Зовётся из конца кадра (`Update`), из seam `DebugTick` и сразу после поднятия
     трубки. `ReflectChildPhone` звук больше не трогает вовсе; заодно она перестала показывать
     трубку под кризисом на seam-пути (в живом кадре её там и не было).
  3. **[MINOR] Гард разводки засчитывал имя события В КОММЕНТАРИИ.** Драйвер комментирован плотно,
     каждое событие в нём названо словами — то есть разводку можно было выпилить целиком, а
     `AudioWiringTests` остался бы зелёным. Сделано два шага:
     • скан идёт по КОДУ — комментарии и строковые литералы вырезаются до поиска (сам автомат под
       гардом `TheScanner_IgnoresCommentsAndStrings`);
     • заведён `AudioBehaviourCoverageTests` (PlayMode): 23 события ПРОГОНЯЮТСЯ ЧЕРЕЗ ЖИВОЙ ДРАЙВЕР
       и спрашиваются журналом `AudioLayer.DebugPlayed`, у каждого назван покрывающий тест;
       остальные 22 перечислены поимённо в списке `ScanOnly` С ПРИЧИНОЙ, почему поведение headless
       дорого. Тест `CoverageSplit_IsExhaustive_AndDisjoint` требует, чтобы два списка давали
       РОВНО перечисление манифеста — новое событие нельзя завести молча, и скан больше не выдаёт
       себя за поведение.
  4. **[MAJOR, найдено по дороге] Попадание в БЛИЦЕ было немым — мёртвый код.** Голос попадания
     стоял внутри `if (_game.HandleInput(input))`, а в блице `Game.HandleInput` ВСЕГДА возвращает
     false (рычаг разбирает ветка кризиса `BlitzPress` и падает в общий `return false`). Итог был
     ровно обратен замыслу, записанному тут же в комментарии: провал звучал (латч `BlitzFails`),
     попадание молчало. Голос блица вынесен наружу accepted-блока; §6-окно и панч плашки остались
     внутри — они по-прежнему не работа по шкале и не ответ.

  Швы, добавленные ради ПОВЕДЕНЧЕСКОЙ проверки (минимальные): `AudioLayer.LoopWanted(channel)` —
  состояние СЛОЯ вместо `AudioSource.isPlaying` (в `-batchmode` устройство может быть пустышкой,
  и движок вернёт «не играю» на честно запущенный луп); журнал `DebugPlayed` для лупов пишет
  СТАРТЫ, а не вызовы (луп сводится с состоянием игры и переспрашивается каждый кадр);
  `DebugTick` теперь крутит и `TickAudioLatches` — иначе события «без события в коде» (купол,
  пульс депрессии) были бы проверяемы только настоящими кадрами по 1/60 с.

## 5. Skeptic verdict

- **Maintainer:** OK (2026-08-08) — два дефекта пойманы ЖИВЫМ плейтестом
  основательницы до гейтов (тишина: сцена без AudioListener — suite был слеп;
  стингер на 100% ответов) — оба закрыты с гардами. Сцена очищена от огрызка
  «One shot audio» (играл цирк PlayOnAwake) — хирургия по .unity, провалидирована
  SceneAudioTests. На ушной гейт: «вата» мягче демо (каскад 1, эталон
  DEMO--1-cotton.mp3); правило «по карточке — только звуки ответа» (Heal/CardDeal
  остаются до её вердикта); импульс-раунд без голоса ответа (класс находки 4 —
  решение за дизайном); визуальная монета рисуется под спецрежимами (не звук).
- **Code:** Codex — 2 MAJOR + 1 MINOR, все закрыты с mutation-proof (монета
  только на принятый тик — accepted-семантика как у панча; единая точка истины
  PhoneRingAudible/SyncPhoneRingLoop; гард разводки: скан без комментариев +
  поведенческий слой на 23 события, 22 — скан с поимённой причиной). Фикс-агент
  нашёл и закрыл 4-й дефект того же класса: попадание в блице было немым
  мёртвым кодом (голос вынесен из accepted-блока по письменному замыслу).
- **Design:** ушной гейт — ЗА ОСНОВАТЕЛЬНИЦЕЙ (громкости черновые, ТЮНИМО в
  одном блоке AudioCatalog); структурная композиция не судится.
- **Гейт headless:** GREEN ×2 — EditMode 454/454 + PlayMode 234/234, честный
  exit 0, свежие каталоги `suite-20260808-133745`, `suite-20260808-133915`.

## 6. Playtest verdict

`Playtest ok:` — (ушной гейт основательницы)
`Landed:` 8d12ab078c984cfb2a2fdbd2f65f76e7e8d5f625 (2026-08-08)
